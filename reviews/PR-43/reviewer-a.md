# Review — reviewer-a

- Verdict: **approve**
- Confidence: 0.62

## Blocking
(none)

## Non-blocking
- {"file":"packages/plugin-uv/src/index.ts","summary":"uv_sync builds its summary and raw from stdout only, but uv writes its human-readable audit lines ('Resolved N packages', 'Audited N packages', 'Installed N packages') to stderr. Real sync runs therefore produce an empty raw and the fallback 'environment synced' summary, losing the model-facing result. The mock test puts 'Audited 0 packages' on stdout, so the suite asserts behavior that does not match real uv.","detail":"uvStatus deliberately concatenates stdout+stderr for parsing; uvSync only reads run.stdout. Fix: include stderr in the summary/raw (and add a real-binary test that asserts the 'Audited' line)."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"uv_status and uv_tree use terminal-text regex parsing instead of machine-readable output, which the plugin standard flags as a last resort. uv tree supports --format json, and sync's dry-run text format varies across uv versions (older uv prints 'Would install N packages' without per-package +/- lines), so 'environment in sync' can be a false negative on version drift.","detail":"The /^[+ -]\\s/ heuristic is fragile; prefer structured output or make the parser tolerant of the 'Would install' header format."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"uv_status and uv_tree are declared 'read' (ungated) but do not pass --no-cache, so they write to the uv cache outside the workspace — a side effect outside the process for a read-class tool. The ruff plugin explicitly passes --no-cache for its read tools.","detail":"Also, the 'offline via --frozen' claim is unverified; --frozen prevents re-resolution but is not the same as --offline. If any cold-cache path hits the index, a read tool performs ungated network I/O."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"uv_python is classified and permission-gated as 'network', but 'uv python find' is a local lookup and does not fetch managed Pythons; the description 'may fetch managed Pythons' is inaccurate for find. This over-gates a read-only-ish operation behind network approval.","detail":"Matches the issue's stated 'python=network+process' spec, so it is per-spec; flagging the inaccurate description and over-gating for a follow-up."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"uv_run gates only on 'process', but 'uv run' syncs the environment first: it creates/updates .venv (workspace write), contacts the package index (network), and executes dependency build hooks (arbitrary code) before the requested command runs. A benign command against an untrusted workspace can therefore trigger network and code execution under a process-only approval.","detail":"Pass --no-sync/--frozen if run should be side-effect-free, or at minimum document the implicit sync in the tool description."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"If the child is killed by an external signal (exitCode null, no error, no timeout/abort), the failure message becomes 'exit code null' because null !== 0 falls into the exitCode branch.","detail":"Minor robustness: handle exitCode === null explicitly."}

## Security
- {"file":"packages/plugin-uv/src/index.ts","summary":"uv_add/uv_remove reject only leading-dash specs; path-like and URL specs are accepted. A spec such as '../sensitive' or 'file:///…' records an out-of-workspace path / arbitrary URL into pyproject.toml, and 'uv add' on a path/git dependency executes the dependency's build backend (arbitrary code). The workspace-boundary check covers projectDir, not the package spec contents.","detail":"Consider restricting packages to package-name-shaped specs (or at least rejecting '..' and absolute/URL prefixes), and document that add/remove run build scripts."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"Combined-class tools are each gated on a single MutationClass: uv_add/uv_remove gate only 'workspace-write' yet fetch over the network; uv_sync gates only 'network' yet writes uv.lock/.venv; uv_run gates only 'process' yet syncs. Under a class-based auto-approval policy (e.g., workspace-write auto-approved, network requiring explicit approval), this is a permission bypass. The issue assigned these classes, so it is a platform-level gap rather than a PR defect, but it should be documented per tool.","detail":"The harness permission-hook API is still 'TBD' per the compatibility manifest; revisit when the host policy is defined."}

## Test gaps
- {"file":"packages/plugin-uv/tests/uv.test.ts","summary":"The uvRunner test helper returns a canned success (exitCode 0, empty output) whenever uv is absent and the cwd exists. Locally without uv, the 'real integration' tests pass on empty output, masking regressions and weakening the TDD RED evidence; only CI (setup-uv) exercises the real binary.","detail":"This also means the assertions on summaries (e.g. /dependency tree/, /python:/) are satisfied by empty output, so they never verify real content locally."}
- {"file":"packages/plugin-uv/tests/uv.test.ts","summary":"uv_add/uv_remove/uv_sync/uv_run have no committed-fixture end-to-end test against the real binary; only mocks. The contract kit's valid-arg runs do hit real uv in CI but depend on network and a Python interpreter ('uv add httpx' may download a managed Python), making CI green-ness network-dependent and flaky.","detail":"Recommend a real add->sync->remove cycle on the fixture with uv in offline/--frozen mode where possible."}
- {"file":"packages/plugin-uv/tests/uv.test.ts","summary":"The dependency-conflict fixture is never exercised against real uv: the 'fails on a dependency conflict' test mocks the runner, so fixtures/uv/conflict is dead weight. Also 'typing-extensions==3.0' is unsatisfiable (no such release), so a real run would fail for the wrong reason.","detail":"Either run real uv against the conflict fixture or drop it."}
- {"file":"packages/plugin-uv/tests/uv.test.ts","summary":"No tests cover the Timeout path, the 10 MiB output-truncation path, or uv_status's out-of-sync summary branch with real uv output; the 'missing pyproject' test also uses a mock rather than a real uv invocation.","detail":"Add mock tests for timedOut/aborted/truncated execution results to lock in the error-code mapping."}

## Compatibility
- {"file":"packages/core/src/process/runner.ts","summary":"The core env allowlist strips HTTP_PROXY/HTTPS_PROXY/ALL_PROXY/NO_PROXY, so uv_sync/uv_add/uv_remove (and uv_run's implicit sync) cannot reach the package index in proxied corporate/sandbox environments. This is the first network-using plugin and surfaces a core-level limitation.","detail":"Consider extending the allowlist with proxy vars for network-capable plugins, or allow per-tool env."}
- {"file":".github/workflows/ci.yml","summary":"setup-uv@v5 installs the latest uv, and metadata declares 'upstreamTool: \"uv\"' with no tested version (the plugin standard asks for tool + version). Flags like 'uv tree --frozen' and 'uv sync --dry-run --frozen', plus the text output format the parser depends on, vary across uv versions — the heuristic parsing can silently break on version drift.","detail":"Pin a uv version in CI and record it in metadata."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"All mutating tools depend on ctx.permission, but the DeepSeek Harness permission-hook API is still 'TBD' per the compatibility manifest; until the host populates it, uv_python/uv_sync/uv_run/uv_add/uv_remove always return PermissionDenied and are unusable in the harness.","detail":"Expected for all plugins; noted so the integration work tracks it."}
- {"file":"packages/core/src/process/runner.ts","summary":"Timeout kills only the direct child (SIGTERM on POSIX, TerminateProcess on Windows), not the process tree. uv_run of a command that spawns grandchildren or ignores SIGTERM can leave orphaned processes and hang until 'close'. A core-level resource concern made reachable by this plugin's process-runner tool.","detail":"Consider a kill-after-graceful-timeout escalation or process-group/job-object termination in core."}

## Architecture
- {"file":"packages/plugin-uv/src/binary.ts","summary":"resolveUvBinary duplicates binary detection in-plugin, though the plugin standard says binary detection must come from @dsh-forge/core (which exports no resolver; ruff/biome follow the same pattern). The PATH scan is also largely redundant since the fallback is the bare name and core's spawn resolves PATH anyway; the scan only risks selecting a stale earlier PATH entry and does blocking statSync per call.","detail":"Consistency fix belongs to core + all plugins together, not this PR."}
- {"file":"packages/plugin-uv/src/index.ts","summary":"uv tools return no diagnostics/summaryBlock; acceptable for this tool family, but the read tools' model-facing output is a bare text summary + raw, which loses the structured dependency-change view the harness could use.","detail":"Minor; not required by the contract kit."}
