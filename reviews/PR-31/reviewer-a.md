# Review — reviewer-a (deepseek via claude cli)

- Verdict: **request_changes**
- Confidence: 0.78

## Blocking
- {"file":"eslint.config.js","line":45,"summary":"ADR-004 no-shell rule is trivially bypassable by the most common child_process forms while SECURITY.md/AGENTS.md claim it is 'enforced by ESLint', and it also false-positives on unrelated bare `exec` calls.","failure_scenario":"The rule returns immediately when `call.callee.type !== 'Identifier'`, so `child_process.exec('rm -rf /')`, `cp.spawn('sh', ['-c', cmd], { shell: true })` with `import * as cp`, and `import { spawn as sp }` all pass lint cleanly. The always-shell `exec`/`execSync` ban — the most security-critical part — is bypassed by the same member-expression form. Non-literal options (`spawn(cmd, [], { shell: process.platform === 'win32' })`), variable options (`const o = {shell:true}; spawn(cmd, [], o)`), and spread options also evade it. Conversely, any locally-defined function named `exec`/`execSync` is flagged unconditionally. No plugin code exists in this PR, but every future plugin PR (e.g. ISSUE-009 ast-grep adapter) is gated by this rule, so a broken security gate ships before the code it is meant to protect.","remediation":"Track actual child_process bindings (ImportSpecifier/ImportNamespaceSpecifier local→imported mapping, MemberExpression property, require destructuring) before reporting, and only flag `exec`/`execSync` when the callee is bound to node:child_process. Add negative tests for the bypass forms above.","severity":"high"}

## Non-blocking
- {"file":"package.json","line":9,"summary":"engines.node is '>=20' and CONTRIBUTING says Node >=20, but the DeepSeek Harness compatibility manifest requires '>=22.19' with upstream CI covering 22.19/24/26.","failure_scenario":"A contributor on Node 20 or 21 gets a green bootstrap toolchain that then fails against the pinned harness; the CI matrix only tests node 22, so 24/26 regressions are invisible.","remediation":"Set engines to >=22.19, update CONTRIBUTING, and add node 24 (and 26 if supported) to the CI matrix to match the manifest."}
- {"file":"package.json","line":8,"summary":"packageManager pins pnpm@11.4.0 while the compatibility manifest pins pnpm@11.7.0 via corepack as the upstream pin.","failure_scenario":"The repo's toolchain and the harness toolchain drift by a patch version; lockfile/reproducibility behavior could diverge from what the manifest was verified against.","remediation":"Align packageManager to pnpm@11.7.0 and re-generate the lockfile, or update the manifest note to document the intentional deviation."}
- {"file":".github/workflows/ci.yml","line":15,"summary":"CI has no timeout-minutes, so a hung install/test/build can consume the full GitHub default (6h) runner time.","failure_scenario":"A hanging step blocks the runner for hours instead of failing fast.","remediation":"Add timeout-minutes (e.g. 15) to the job."}
- {"file":"package.json","line":34,"summary":"'clean' runs `pnpm -r exec rimraf dist coverage || true`, but rimraf is not a declared dependency; the `|| true` masks the failure so clean silently does nothing.","failure_scenario":"`pnpm clean` exits 0 while dist/coverage are left in place whenever rimraf is not present in node_modules/.bin.","remediation":"Declare rimraf as a devDependency or use a Node one-liner / platform-agnostic rm."}

## Security
- {"file":"eslint.config.js","line":45,"summary":"The ADR-004 gate can be bypassed by member-expression, namespace-import, renamed-import, variable-options, and non-literal-shell forms; the always-shell `exec`/`execSync` ban is bypassed by `cp.exec(...)`. Docs overstate the enforcement.","failure_scenario":"A plugin does `import * as cp from 'node:child_process'; cp.spawn('sh', ['-c', userInput], { shell: true })` and CI passes, violating the project's stated non-negotiable security invariant.","remediation":"Bind-aware rule (track imports/require destructuring and member-expression property names) plus negative tests."}
- {"file":"eslint.config.js","line":31,"summary":"The rule unconditionally flags any function named exec/execSync/spawn/etc. even when not from node:child_process, producing false positives that could push developers to disable the rule.","failure_scenario":"A local helper `function exec(cb) { cb(); }` is blocked from merging for a shell violation it cannot commit.","remediation":"Gate reporting on actual node:child_process bindings rather than identifier name alone."}

## Test gaps
- {"file":"tests/eslint-adr004.test.ts","line":83,"summary":"The ADR-004 tests cover only direct bare-identifier calls; there are no negative tests for member-expression (`cp.spawn`), renamed imports (`spawn as sp`), variable options (`{shell:true}` via identifier), non-literal shell values, or spread options — all of which currently bypass the rule.","failure_scenario":"The exact bypasses a future plugin could use are unexercised, so CI cannot catch the rule regressing to a no-op for those forms.","remediation":"Add the bypass forms as expected-to-be-flagged test cases (they will fail today, driving the rule fix)."}
- {"file":".github/workflows/ci.yml","line":30,"summary":"Neither `format:check` nor `coverage` runs in CI even though format and coverage are stated objectives of the bootstrap issue.","failure_scenario":"Unformatted code and 0% coverage both merge with green CI; the 'format' and 'coverage' toolchain deliverables are not actually enforced.","remediation":"Add `pnpm format:check` to CI and run coverage (at least non-gating first, then a threshold once meaningful code exists)."}

## Compatibility
- {"file":"package.json","line":9,"summary":"engines '>=20' and CONTRIBUTING 'Node.js >= 20' contradict the compatibility manifest's node_requirement '>=22.19'; CI matrix covers only node 22.","failure_scenario":"Development on Node 20/21 is permitted by the toolchain but incompatible with the pinned DeepSeek Harness.","remediation":"Raise engines/CONTRIBUTING to >=22.19 and extend the matrix to node 24/26 per the manifest."}
- {"file":"package.json","line":8,"summary":"pnpm@11.4.0 pinned in the repo vs pnpm@11.7.0 pinned upstream per the manifest.","failure_scenario":"Toolchain drift relative to the harness's verified package-manager version.","remediation":"Align the pnpm pin with the manifest (11.7.0) or document the deviation."}
- {"file":"AGENTS.md","line":34,"summary":"AGENTS.md instructs agents to verify against `compatibility/deepseek-harness.json`, but that file does not exist in this PR (it is created by a later issue).","failure_scenario":"An agent following AGENTS.md looks for a pinned-commit manifest that is absent, and the pinned commit reference exists only outside the repo.","remediation":"Create the manifest in this bootstrap PR or note it as pending ISSUE-002 to avoid a dangling reference."}

## Architecture
- {"file":"packages/core/package.json","line":8,"summary":"main/types point to src/index.ts while publishConfig points to dist; workspace consumers always resolve raw TS source.","failure_scenario":"Anything importing the built @dsh-forge/core artifact outside vitest (e.g. the harness host at runtime) would attempt to load a .ts file and fail; only the vitest/tooling path is exercised.","remediation":"Acceptable for bootstrap, but add an `exports` field or a smoke test that consumes the built dist to lock the intended runtime resolution."}
- {"file":"tsconfig.json","line":11,"summary":"`types: [\"vitest/globals\"]` is configured while all tests explicitly import from 'vitest', making the global types declaration redundant.","failure_scenario":"No functional break, but the config suggests globals are available when the codebase deliberately avoids them.","remediation":"Drop `vitest/globals` (or drop the explicit imports) to keep the two consistent."}
