# Review — reviewer-a

- Verdict: **approve**
- Confidence: 0.78

## Blocking
(none)

## Non-blocking
- {"file":"packages/plugin-docker/src/index.ts","line":303,"summary":"docker_logs concatenates stdout+stderr with no interleaving/separator, and okResult truncates the combined string from the start at 20K — so trailing stderr (often where app loggers write) can be silently dropped after the cap.","failure_scenario":"A container writes 15K to stdout and 15K of error lines to stderr; combined raw is sliced to the first 20K, discarding the final ~10K of stderr where the most relevant errors live."}
- {"file":"packages/plugin-docker/src/index.ts","line":296,"summary":"docker_logs accepts any positive integer tail, but Number.isInteger(1e100) is true and String(1e100) === \"1e+100\", which docker rejects — a model-passed large number yields a spurious ToolFailure instead of a clean InvalidArguments or a bounded run.","failure_scenario":"args { name: 'web', tail: 1e100 } passes validation and name/tail checks, spawns `docker logs --tail 1e+100 web`, and fails with ToolFailure rather than an argument error."}
- {"file":"packages/plugin-docker/src/index.ts","line":96,"summary":"redactCredentials second regex (user:pass@) can false-positive on ordinary log/error text like 'ERROR: service@host down', mangling diagnostics (display-only, not raw).","failure_scenario":"A docker stderr line 'connect failed: redis@cache down' is rendered as 'redis:***@cache down', degrading the model-facing error message."}
- {"file":"packages/plugin-docker/src/index.ts","line":388,"summary":"docker_ps/docker_images have no row cap (e.g. `docker ps --limit`), so a large daemon hits the 10 MiB output cap and the tool returns ToolFailure instead of a bounded partial listing.","failure_scenario":"A host with many containers/images makes `docker ps -a` / `docker images` exceed 10 MiB; the tool fails wholesale rather than returning the first N rows."}
- {"file":"packages/plugin-docker/src/binary.ts","line":50,"summary":"Binary resolution only considers `<name>.exe`/`<name>` on Windows; PATHEXT shims like docker.cmd/docker.bat are never found, yielding BinaryNotFound despite a usable CLI. Docker Desktop ships docker.exe so impact is low.","failure_scenario":"A Windows host with only a docker.cmd shim on PATH gets BinaryNotFound instead of running the tool."}

## Security
- {"file":"packages/plugin-docker/src/index.ts","line":210,"summary":"The plugin passes no env to ctx.run and the ExecutionRequest shape exposed in tests has no env field, so env allowlisting is entirely core's responsibility. If core's runner defaults to full inherited env, docker/compose inherit the harness environment (including secrets and DOCKER_HOST/DOCKER_CONTEXT), letting a repo .env redirect compose to a different daemon. Must be verified against the core runner.","failure_scenario":"If core passes process.env through when env is omitted, `docker compose ps` could pick up DOCKER_HOST/DOCKER_CONTEXT and query an unintended daemon; third-party CLI receives the full inherited environment, violating the plugin standard's env-allowlist rule."}
- {"file":"packages/plugin-docker/src/index.ts","line":224,"summary":"The missing-binary contract depends on core mapping a spawn-ENOENT of the random sentinel path to error.code 'BinaryNotFound'; the unit test mocks this instead of exercising it. If core maps it to a generic ToolFailure, the sentinel path leaks into the model-facing message and the BinaryNotFound contract silently breaks.","failure_scenario":"A host without docker triggers `ctx.run` on the random sentinel; if core returns ToolFailure (not BinaryNotFound), the error exposes a temp path and the contract suite's missing-binary check would catch it only if that check uses the real runner."}
- {"file":"packages/plugin-docker/src/index.ts","line":356,"summary":"docker_logs and docker_inspect return unredacted raw output (container logs / Config.Env) by design, while the security policy states sensitive values are redacted from captured output — the policy is only applied to error diagnostics, not raw, which should be a documented intentional carve-out for these tools.","failure_scenario":"docker_inspect on a container with secrets in Config.Env returns them in raw; acceptable for an inspection tool, but inconsistent with the stated 'sensitive values are redacted from captured output' policy."}

## Test gaps
- {"file":"packages/plugin-docker/tests/docker.test.ts","line":223,"summary":"No test exercises the timedOut / aborted / truncated / generic exec.error branches of runDocker — these error paths are core to the tool's robustness claims but are never asserted.","failure_scenario":"A future refactor of runDocker misorders the exec.error/timedOut checks ships green because no test covers them."}
- {"file":"packages/plugin-docker/tests/docker.test.ts","line":290,"summary":"docker_compose_status's directory branch (--project-directory) is never tested; only the file (-f) path, violations, leading-dash rejection, and symlink-loop are covered.","failure_scenario":"A regression in the isDir detection or argv construction for directories goes unnoticed because no test passes a directory path."}
- {"file":"packages/plugin-docker/tests/docker.test.ts","line":290,"summary":"docker_compose_status default (no path) invocation is untested.","failure_scenario":"Workspace-root compose discovery behavior could regress without test coverage."}
- {"file":"packages/plugin-docker/tests/docker.test.ts","line":215,"summary":"No direct tests for redactCredentials (positive redaction of userinfo URLs, negative case that ordinary text is untouched).","failure_scenario":"The regexes could be tightened/broken in a refactor with no test signal."}
- {"file":"packages/plugin-docker/tests/docker.test.ts","line":271,"summary":"docker_inspect malformed-stdout → ParseFailure path and docker_images empty output are untested.","failure_scenario":"parseJsonOutput failure handling regresses without coverage."}
- {"file":"packages/plugin-docker/tests/docker.test.ts","line":468,"summary":"The live compose test asserts only that output parses as an array, not that it reflects the fixture services; the real ENOENT→BinaryNotFound mapping is never exercised end-to-end because hasRealDocker gating skips when docker is absent.","failure_scenario":"Core could change its ENOENT mapping and this plugin's suite would stay green."}
- {"file":"packages/plugin-docker/tests/docker.test.ts","line":160,"summary":"docker_ps with all:false is never tested (only the default -a path).","failure_scenario":"The all===false branch could regress unnoticed."}

## Compatibility
- {"file":"packages/plugin-docker/src/index.ts","line":545,"summary":"The compatibility manifest states the upstream DeepSeek Harness Cordis plugin registration API is 'TBD — must be verified against the pinned commit'. This plugin exports the repo's own {metadata, tools} Plugin Standard shape; actual registration compatibility with the pinned upstream harness is still unverified.","failure_scenario":"If the harness expects a Cordis-style plugin export, this package will not load in the real host even though the contract suite passes."}
- {"file":"packages/plugin-docker/src/index.ts","line":421,"summary":"docker_compose_status requires the compose v2 plugin (`docker compose`); there is no fallback to the standalone `docker-compose` v1 binary, so environments with only v1 get ToolFailure.","failure_scenario":"A host with docker + docker-compose v1 only: docker_status/ps/images work but docker_compose_status always fails."}
- {"file":"packages/plugin-docker/src/binary.ts","line":22,"summary":"Relative PATH entries are deliberately skipped, and only .exe/docker names are tried on Windows — installs reachable only via a relative PATH entry or a PATHEXT shim resolve to BinaryNotFound (low impact for Docker Desktop).","failure_scenario":"PATH contains an entry like '.\\bin' holding docker; resolution fails and the tool reports BinaryNotFound despite docker being invocable."}
- {"file":"packages/plugin-docker/package.json","line":7,"summary":"main/types point at ./src/index.ts rather than a build output; workspace TS resolution works, but a published artifact would break consumers without a TS loader.","failure_scenario":"npm pack + install of the package fails to resolve the entrypoint at runtime."}

## Architecture
- {"file":"packages/plugin-docker/src/binary.ts","line":1,"summary":"Binary detection is implemented in the plugin despite the 'No infrastructure duplication: binary detection comes from @dsh-forge/core' rule. The absolute-path + unpredictable-sentinel invariants are sound and well-commented, but if core exposes an equivalent detection API the plugin should use it rather than owning this logic.","failure_scenario":"Security invariants for binary resolution drift between core and plugins; reviewers cannot rely on one canonical implementation."}
- {"file":"packages/plugin-docker/src/index.ts","line":96,"summary":"Credential redaction is implemented locally; if core already provides a redaction/normalization utility, this duplicates it and risks inconsistent redaction across plugins.","failure_scenario":"A future core redaction fix does not propagate to this plugin's copy."}
