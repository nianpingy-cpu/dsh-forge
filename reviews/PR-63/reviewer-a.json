{
  "verdict": "request_changes",
  "blocking": [
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":115,\"summary\":\"Missing-worker BinaryNotFound is unreachable through the real runner: the random sentinel from resolveVisionWorker() is passed as argv[0] to process.execPath, so a missing worker script makes node itself spawn fine, print a module-not-found error to stderr and exit 1 with empty stdout. exec.error is never set, the BinaryNotFound branch (line 127) is skipped, and parseWorker converts the empty stdout into ParseFailure. This violates TDD criterion #5 and Plugin Standard rule 4 (Missing binary => BinaryNotFound). The unit test masks it by mocking runner error={code:'BinaryNotFound'}, which the real runProcess never produces for a missing script argument.\",\"detail\":\"Fix: check existsSync() on the resolved worker path before ctx.run and return BinaryNotFound directly, or map (exitCode!==0 && stdout empty) for this specific tool to BinaryNotFound.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":663,\"summary\":\"data_analyze computes column stats with Math.min(...nums)/Math.max(...nums) over the full column array. For files above roughly 100k-200k rows — well within the 64 MiB input cap and above the 'large-dataset' diagnostic the worker itself emits at >100k rows — the spread throws RangeError: Maximum call stack size exceeded, surfacing as an opaque ToolFailure. Separately, for a mixed column that is >80% numeric, nums retains null for the non-numeric cells; Math.min coerces null to 0 and mean divides by the full column length, silently corrupting min/mean/stddev.\",\"detail\":\"Fix: iterate with a reduce/for-loop instead of spread, and filter nulls out of nums before computing min/max/mean/variance (e.g. nums = vals.map(toNumberValue).filter((v): v is number => v !== null)).\"}"
  ],
  "non_blocking": [
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":236,\"summary\":\"Worker-reported error codes (UnsupportedFormat, ParseFailure, InvalidArguments) are flattened to generic ToolFailure; only the message preserves the inner code, losing normalized error fidelity for callers/tests.\"}",
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":477,\"summary\":\"series schema has minItems but no maxItems; a very large inline series becomes a huge --series argv string that can exceed OS argv limits (E2BIG), yielding an opaque ToolFailure instead of a clean validation error. Add maxItems: 2000 (matching the worker cap) and cap label lengths.\"}",
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":385,\"summary\":\"vision_inspect task and chart title have no length cap; very long strings bloat argv and are unbounded input. Add a max-length guard in isValidTextInput.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":890,\"summary\":\"Pie chart with a single data point renders an invisible chart (start==end arc degenerates); negative pie values are folded in via Math.abs without documenting that proportions use absolute values.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":820,\"summary\":\"scatter type is not a real scatter plot: x positions are index-spaced and only (label,value) pairs are accepted, so genuine x/y scatter data cannot be rendered.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":720,\"summary\":\"Negative bar values draw the rect below the baseline, outside the plot area and overlapping the x-axis labels.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":547,\"summary\":\"UTF-8 BOM is not stripped from CSV input, so the first column name can carry a leading \\\\uFEFF.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":441,\"summary\":\"Format is inferred purely from the file extension: a CSV named .json fails, and JSON named .csv is parsed as garbage CSV. No content sniffing.\"}",
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":342,\"summary\":\"chart_generate does not create missing parent directories, so output like 'charts/sales.svg' fails with ENOENT -> ToolFailure even though the path is a valid workspace target.\"}",
    "{\"file\":\"packages/plugin-vision/src/binary.ts\",\"line\":33,\"summary\":\"The sentinel branch of resolveVisionWorker() is never exercised by any test; the test named 'returns an absolute sentinel when the worker is absent' actually asserts on the present path.\"}",
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":525,\"summary\":\"No dedicated test covers chart_generate output-path symlink escape (input symlink is covered; output symlink relies on core canonicalize untested by this PR).\"}"
  ],
  "security": [
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":342,\"summary\":\"TOCTOU on the overwrite guard: existsSync(absolute) in the plugin process and writeFileSync in the worker process are separate operations; a concurrent symlink swap after resolution can redirect the write outside the workspace. Core's canonicalize mitigates pre-existing symlinks but not a post-resolution swap. Inherent to path-based writes, but worth a re-check (O_NOFOLLOW or open-parent-fd) if the workspace is untrusted.\"}",
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":562,\"summary\":\"Unbounded --series/--task argv can exhaust OS argv space (E2BIG) from a single tool call; handled as ToolFailure, so it is a resource-availability concern rather than a crash, but the schema should cap it.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":66,\"summary\":\"escapeXml escapes <>&'\\\" but not control characters; series labels (not validated for control chars by the plugin) with \\\\x01-style bytes produce invalid XML 1.0 in the SVG. NUL bytes are already blocked at spawn. Low impact.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":100,\"summary\":\"The backend-unavailable diagnostic tells users to 'configure DSH_VISION_API_KEY' to enable defect recognition, but no vision backend exists and DSH_VISION_API_KEY is not in the env allowlist — the hint is inert and misleading.\"}"
  ],
  "test_gaps": [
    "{\"file\":\"packages/plugin-vision/tests/vision.test.ts\",\"line\":200,\"summary\":\"No test runs data_analyze on a large (>100k-row) or mixed-type column file, so the Math.min spread crash and null-coercion stat corruption are undetected.\"}",
    "{\"file\":\"packages/plugin-vision/tests/vision.test.ts\",\"line\":210,\"summary\":\"BinaryNotFound is only tested with a mocked runner error; no test exercises the real runner against a genuinely absent worker script, which is exactly where the bug lives.\"}",
    "{\"file\":\"packages/plugin-vision/tests/vision.test.ts\",\"line\":150,\"summary\":\"No coverage for output-path symlink escape, BOM-prefixed CSV, CRLF-only line endings, quoted embedded newlines, or truncated/malformed PNG/JPEG/WebP headers.\"}",
    "{\"file\":\"packages/plugin-vision/tests/vision.test.ts\",\"line\":355,\"summary\":\"No test passes a --task through vision_inspect to assert the backend-unavailable diagnostic path.\"}"
  ],
  "compatibility": [
    "{\"file\":\"packages/plugin-vision/README.md\",\"line\":1,\"summary\":\"README states Node >= 20 while the DeepSeek Harness compatibility manifest pins node_requirement >= 22.19 (upstream CI 22.19/24/26). Not conflicting (superset), but the plugin README should align to the manifest's floor.\"}",
    "{\"file\":\"packages/plugin-vision/package.json\",\"line\":1,\"summary\":\"Runtime depends on committed scripts/vision-worker.mjs shipping in the published package; it ships today only because package.json has no files field. The SECURITY.md 'packages ship only tsup artifacts' allowlist narrative would drop it if ever enforced beyond dist/ (checkContents currently scans dist only), silently breaking every tool.\"}",
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":598,\"summary\":\"coreContractVersion '1.0.0' matches CORE_VERSION; presets 'full' correctly registers the plugin. Defaulting permission to denied when ctx.permission is absent is a safe default, but means the harness must populate ctx.permission for chart_generate to ever run.\"}"
  ],
  "architecture": [
    "{\"file\":\"packages/plugin-vision/src/index.ts\",\"line\":1,\"summary\":\"Structure matches the plugin standard: typed argv, MutationClass on each tool, execution/workspace/diagnostics delegated to core, no shell, no bare-name exec. The random-UUID sentinel is actually more correct than plugin-biome's bare-name PATH fallback.\"}",
    "{\"file\":\"packages/plugin-vision/scripts/vision-worker.mjs\",\"line\":1,\"summary\":\"~950 lines of hand-rolled PNG/JPEG/WebP/BMP/CSV/SVG parsing live inside the plugin rather than as a vetted dependency; this is the largest hand-rolled parsing surface in the repo and the source of the correctness defects found. Consider delegating to battle-tested parsers (e.g. sharp for images, a CSV library) or reducing scope.\"}"
  ],
  "confidence": 0.85
}