# Review — reviewer-a (deepseek via claude cli)

- Verdict: **approve**
- Confidence: 0.72

## Blocking
(none)

## Non-blocking
- {"file":"packages/core/src/workspace/policy.ts","line":38,"summary":"TOCTOU on non-existent tail: boundary check validates the deepest existing ancestor, but components of the not-yet-existing tail are re-appended verbatim; a symlink planted there between check and write redirects the write outside the workspace.","failure_scenario":"resolveInWorkspace(root, 'sub/new/f.txt') with 'sub' nonexistent returns root/sub/new/f.txt (contained). Before the caller's write, an attacker creates root/sub -> /outside. The write to root/sub/new/f.txt follows the symlink and lands outside the workspace."}
- {"file":"packages/core/src/workspace/policy.ts","line":56,"summary":"Workspace rooted at a filesystem root breaks the boundary check: 'rootCanonical + sep' becomes '//' (POSIX) or 'C:\\\\' (Windows), so every target fails the startsWith test.","failure_scenario":"resolveInWorkspace('/', '/etc/passwd') throws WorkspaceViolationError even though /etc/passwd is inside '/' — a false denial, not an escape, but the containment predicate is wrong for a valid workspace root."}
- {"file":"packages/core/src/workspace/policy.ts","line":76,"summary":"canonicalize rethrows raw ErrnoException for EACCES/ELOOP/ENOTDIR instead of a normalized error, so callers must handle non-WorkspaceViolationError throws; ENOTDIR (a parent component is a regular file) is not converted to a clean violation.","failure_scenario":"resolveInWorkspace(root, 'src/a.ts/sub.txt') where src/a.ts is a file throws ENOTDIR; the caller catches only WorkspaceViolationError and treats it as an unexpected crash rather than a boundary denial."}
- {"file":"packages/core/src/workspace/policy.ts","line":99,"summary":"isDanglingSymlink is misnamed — it returns true for any symlink, dangling or not. Correct today only because it is consulted exclusively in the ENOENT branch, but it is a readability/maintenance hazard.","failure_scenario":"A future caller uses isDanglingSymlink outside the ENOENT path and rejects valid non-dangling links, or a refactor moves it into a path where a resolvable link is misjudged."}
- {"file":"packages/core/src/workspace/policy.ts","line":87,"summary":"OperationDescriptor.delete carries no target, so the descriptor cannot express the delete's boundary check; the write/delete boundary enforcement is left entirely to the caller to pair manually with resolveInWorkspace.","failure_scenario":"A tool author uses classifyMutation({kind:'delete'}) to gate a delete but forgets to run the target through resolveInWorkspace; the delete proceeds against an unvalidated path."}

## Security
- {"file":"packages/core/src/workspace/policy.ts","line":102,"summary":"Delete classification is fail-open: classifyMutation({kind:'delete', irreversible:false}) — and any runtime value that is falsy/omitted in JS — classifies as workspace-write, bypassing the destructive guard entirely. Unknown irreversibility should default to destructive (fail-closed) or the field should be required and enforced at runtime.","failure_scenario":"A caller that permanently removes a workspace file classifies it as reversible (or omits the field in JS) and gets workspace-write; assertPermission never consults destructiveAllowed, so an irreversible delete proceeds without the explicit destructive guard the issue requires."}
- {"file":"packages/core/src/workspace/policy.ts","line":36,"summary":"The module's docstring overclaims TOCTOU protection: returning the canonical path closes the race only when the symlink already exists at validation time, not when the attacker creates a symlink in the non-existent tail between validation and use. The guarantee should be stated precisely and callers must be told to create parent dirs exclusively / verify immediately before write.","failure_scenario":"See non_blocking TOCTOU entry — write escapes workspace via a tail symlink created after validation."}
- {"file":"packages/core/src/workspace/policy.ts","line":42,"summary":"resolveInWorkspace and assertPermission are standalone and nothing composes them: a workspace-write can receive permission approval without ever running the boundary check. The 'no write operation bypasses boundary check' criterion is not enforced by any single API.","failure_scenario":"A tool author checks assertPermission('workspace-write', {approved:true}) and skips resolveInWorkspace; a path with ../../ escape is written outside the workspace despite the permission policy passing."}

## Test gaps
- {"file":"packages/core/tests/workspace-policy.test.ts","line":99,"summary":"The case-collision test is not portable: on macOS default case-insensitive APFS, alt is the same directory as root (mkdirSync succeeds idempotently, realpath resolves to the same node), so the expected throw does not occur and the test fails. The skip guard covers only win32.","failure_scenario":"Running pnpm test on a default Mac fails this assertion even though the implementation is correct for that filesystem."}
- {"file":"packages/core/tests/workspace-policy.test.ts","line":29,"summary":"Positive tests assert equality with non-canonical resolve(root,...), which fails on platforms where the temp dir path contains a symlink (e.g., macOS /tmp -> /private/tmp, /var -> /private/var) because resolveInWorkspace returns the canonicalized /private/... path. Linux+Windows CI masks this.","failure_scenario":"On macOS, tmpdir() under /var/... realpaths to /private/var/..., so expect(resolved).toBe(resolve(root,'src','a.ts')) fails."}
- {"file":"packages/core/tests/workspace-policy.test.ts","line":28,"summary":"No positive test for a non-existent nested leaf inside the workspace (e.g., resolveInWorkspace(root,'new/deep/file.txt')), so the tail re-append slicing in canonicalize — the most subtle code path — is only exercised through rejection paths.","failure_scenario":"A regression in the tail-slicing (wrong separator, dropped component) would go uncaught because no test asserts the canonical form of a write to a not-yet-existing path."}
- {"file":"packages/core/tests/workspace-policy.test.ts","line":77,"summary":"No test for classifyMutation({kind:'delete'}) with irreversible omitted/falsy, which would have pinned the intended fail-open-vs-fail-closed semantics; also no test for assertPermission('destructive', {approved:false, destructiveAllowed:true}) which returns false without throwing.","failure_scenario":"The delete default classification can change or regress without a test noticing; the non-throwing destructive denial branch is untested."}
- {"file":"packages/core/tests/workspace-policy.test.ts","line":71,"summary":"No Windows tests for drive-relative (C:evil.txt), root-relative (\\evil.txt), or cross-drive absolute (D:\\evil.txt) targets, which exercise the messiest isAbsolute/resolve behavior; the backslash test covers only one form.","failure_scenario":"A drive-relative target resolving to the wrong drive would be missed; containment currently catches it, but nothing locks that in."}

## Compatibility
- {"file":"packages/core/src/workspace/policy.ts","line":107,"summary":"PermissionContext.approved / destructiveAllowed is an invented interface; the DeepSeek Harness permission_hook_api is explicitly 'TBD — must be verified against the pinned commit' in the compatibility manifest. assertPermission cannot be claimed DSH-compatible until that API is verified.","failure_scenario":"When ISSUE-010 integration maps DSH's real permission hook into this module, the context shape may not carry the required fields, forcing a breaking API change."}
- {"file":"packages/core/src/workspace/policy.ts","line":53,"summary":"On Windows, realpathSync can return \\\\?\\-prefixed long-path forms for deep paths; if the workspace root is short but a candidate crosses the long-path threshold, rootCanonical and canonical use different representations and the prefix check falsely rejects a contained write.","failure_scenario":"Workspace at C:\\ws (short) with a write target under a path exceeding ~260 chars realpaths to \\\\?\\C:\\...; startsWith(C:\\ws\\) fails → legitimate write denied (denial, not escape)."}

## Architecture
- {"file":"packages/core/src/workspace/policy.ts","line":42,"summary":"The policy module is not wired into any write path in this repo; the acceptance criterion 'No write operation bypasses boundary check' is vacuous until an adapter or plugin executes writes through resolveInWorkspace + assertPermission in one path.","failure_scenario":"No current code calls these functions outside tests, so a future plugin can bypass the boundary entirely without this PR catching it."}
- {"file":"packages/core/src/workspace/policy.ts","line":118,"summary":"assertPermission has asymmetric control flow — returns boolean for approval denials but throws DestructiveOperationError for the guard — so callers must both check the return and wrap in try/catch; a caller that only checks the boolean leaves destructive denials as uncaught exceptions.","failure_scenario":"if (assertPermission('destructive', ctx)) {...} without try/catch crashes on the guard check instead of denying gracefully; a try/catch that swallows the error could mislabel the outcome."}
- {"file":"packages/core/src/workspace/policy.ts","line":90,"summary":"Mutation classes are meant to be declared statically per tool in the Plugin Standard, yet classifyMutation is a runtime descriptor mapper; keeping both paths invites drift between declared metadata and runtime classification.","failure_scenario":"A tool declares mutationClass:'destructive' in metadata but routes through classifyMutation({kind:'delete'}) and lands on workspace-write, giving the model a lower-privilege impression than the declared intent."}
