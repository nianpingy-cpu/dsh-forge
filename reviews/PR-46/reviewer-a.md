# Review — reviewer-a

- Verdict: **approve**
- Confidence: 0.62

## Blocking
(none)

## Non-blocking
- {"file":"packages/plugin-trivy/src/index.ts","line":380,"summary":"parseReport rejects Results===null as ParseFailure, but the clean-scan JSON shape is never verified against real trivy; if any trivy version marshals an empty Results as null (Go nil slice), every clean scan becomes a false ParseFailure.","failure_scenario":"trivy emits {\"SchemaVersion\":2,\"ArtifactName\":\"x\",\"Results\":null} for a clean image/fs scan; parseReport sees Results !== undefined and !Array.isArray(null) → ParseFailure instead of 'no findings'.","category":"correctness"}
- {"file":"packages/plugin-trivy/src/index.ts","line":105,"summary":"toRelativeFile strips by startsWith(workspaceRoot) with no separator boundary and is case-sensitive: a sibling path sharing the prefix is mis-stripped, and on Windows (case-insensitive fs, trivy emits mixed / and \\ separators) the prefix fails and absolute paths leak into diagnostics[].file.","failure_scenario":"workspaceRoot=/tmp/dsh-trivy-abc, Target=/tmp/dsh-trivy-abcXYZ/f.ts → file becomes 'XYZ/f.ts'; on Windows a C:/ vs C:\\ casing mismatch returns the absolute path unchanged.","category":"correctness"}
- {"file":"packages/plugin-trivy/src/index.ts","line":745,"summary":"Whitespace-only repo/path inputs pass validation (empty check runs on the untrimmed string) and resolve to the workspace root, triggering an unintended full-workspace scan instead of InvalidArguments.","failure_scenario":"execute({repo:'   '}) → target='' → resolveInWorkspace(root,'') returns root → trivy repo scans the entire workspace.","category":"correctness"}
- {"file":"packages/plugin-trivy/src/index.ts","line":80,"summary":"trivyError surfaces only the first non-empty stderr line; trivy writes progress lines ('Downloading vulnerability DB…') to stderr, so a benign progress line can mask the real error in the ToolFailure message.","failure_scenario":"trivy fails on DB download; first stderr line is 'Downloading vulnerability DB...' and the actual FATAL line is later → model sees only the progress line.","category":"correctness"}
- {"file":"packages/plugin-trivy/src/index.ts","line":490,"summary":"redactCredentials can corrupt legitimate non-credential content: the user:pass@ pattern rewrites image digests and any Target containing 'word:@' (e.g. repo:tag@sha256:… → repo:***@sha256:…). Cosmetic (raw is reference-only) but silently alters data the model may rely on.","failure_scenario":"ArtifactName 'alpine:3.19@sha256:abc' in raw output becomes 'alpine:***@sha256:abc'.","category":"correctness"}
- {"file":"packages/plugin-trivy/src/index.ts","line":220,"summary":"Tool descriptions promise flags the inputSchema does not expose ('use --skip-policy-update via local flags for offline runs'; trivy_sbom says 'Generate/scan') — users cannot pass any flags, so the documented offline path is unimplementable and the sbom description overstates the tool.","failure_scenario":"A user following the description tries to run an offline config scan and has no input field to do so.","category":"architecture"}

## Security
- {"file":"packages/plugin-trivy/src/index.ts","line":758,"summary":"trivy_repo_scan forwards arbitrary ssh://, git@, and git:// URLs to trivy's clone path; the leading-dash guard only rejects strings starting with '-' and is bypassed by scheme-embedded option strings (ssh://-oProxyCommand=…/). Whether this is exploitable depends on trivy shelling out to git/ssh vs using go-git, which the plugin does not control.","failure_scenario":"A crafted repo URL 'ssh://-oProxyCommand=<cmd>@host/x' passes the startsWith('-') check and reaches trivy; if trivy ever shells out to git/ssh, the authority segment is parsed as an option. Recommend restricting remote repo schemes to http(s)/git or validating the URL host."}
- {"file":"packages/plugin-trivy/src/index.ts","line":223,"summary":"The core env allowlist excludes HTTP_PROXY/HTTPS_PROXY/NO_PROXY, so trivy's vulnerability-DB, policy-bundle, and image-pull downloads cannot work behind a corporate proxy, and the plugin provides no way to inject proxy/registry credentials; network-class tools silently fail or hang until timeout in proxied environments.","failure_scenario":"CI/dev behind an egress proxy: trivy config/repo/image/sbom first-run DB download never connects → 300s timeout → Timeout tool error, every time."}
- {"file":"packages/plugin-trivy/src/index.ts","line":660,"summary":"Secret redaction covers Secrets[].Match and top-level CauseMetadata.Code, but not nested code-bearing fields (e.g. CauseMetadata.Occurrences[].Code in newer trivy) nor any future secret fields; raw for repo/config/sbom scans is rebuilt only from what redactReportObject covers, so a shape change could leak plaintext secret snippets to the model.","failure_scenario":"trivy adds an additional code-snippet field under a secret/misconfig finding; redactReportObject misses it and JSON.stringify emits the plaintext secret into raw."}

## Test gaps
- {"file":"packages/plugin-trivy/tests/trivy.test.ts","line":816,"summary":"No test exercises a real zero-finding scan. All live integration tests (config/secret/license/repo) use fixtures with findings, and the parser tests mock clean output as {Results: []} — a shape real trivy (omitempty) never emits. The clean-scan path (absent Results) and the Results===null rejection are unverified against trivy v0.74.0.","failure_scenario":"The clean-scan JSON shape is wrong in either direction and no test catches it: a clean scan either ParseFailures or a malformed report is reported as 'no findings'."}
- {"file":"packages/plugin-trivy/tests/trivy.test.ts","line":816,"summary":"No tests for the timeout→Timeout mapping, truncated-output→ToolFailure cap, or SpawnFailure (execution.error) paths, even though these are distinct branches in runTrivy.","failure_scenario":"A regression in the timedOut/truncated handling (e.g. a timed-out trivy returning partial JSON reported as findings) would go uncaught."}
- {"file":"packages/plugin-trivy/tests/trivy.test.ts","line":598,"summary":"The neutral-cwd test asserts only captured.cwd matches the runtime dir; it never verifies trivy actually ignores a repo-planted .trivyignore/.trivyignore.yaml (the security property the mitigation exists for).","failure_scenario":"If trivy v0.74 also reads .trivyignore from the scanned target root, a repo-planted ignore file silently suppresses findings and the test still passes."}
- {"file":"packages/plugin-trivy/tests/trivy.test.ts","line":816,"summary":"No test covers an image ref or repo URL containing a digest/userinfo beyond one tokenized-URL case, and no test covers the whitespace-only repo/path bypass.","failure_scenario":"Whitespace-only or digest-bearing inputs behave unexpectedly (full-workspace scan / mangled raw) with no test documenting the intended behavior."}

## Compatibility
- {"file":"packages/plugin-trivy/src/index.ts","line":855,"summary":"metadata.upstreamTool: 'trivy' carries no version; CI pins trivy v0.74.0 but the plugin does not declare the tested version, so a future trivy report-shape change (fields, Results null-vs-absent, rule IDs like DS-0001) could break parsing silently and nothing pins or documents the expected shape contract.","failure_scenario":"trivy v0.75 renames a report field; the plugin's strict parseReport still passes for the fields it reads but mis-parses or drops findings with no signal."}
- {"file":".github/workflows/ci.yml","line":47,"summary":"trivy is installed only on Linux CI, so all real-binary integration tests are Linux-only; Windows-specific behavior (path separators in Target, CRLF stderr, .exe resolution, tmpdir cleanup races) is never exercised in CI.","failure_scenario":"On Windows, trivy reports Targets with C:/ separators while workspaceRoot uses C:\\ → startsWith fails → absolute paths in diagnostics; uncaught because Windows CI skips the live tests."}

## Architecture
- {"file":"packages/plugin-trivy/tests/trivy.test.ts","line":467,"summary":"Real-binary integration tests (config/secret/license) run unconditionally whenever trivy is installed, contradicting the issue's 'live scans only in opt-in integration lane' requirement; the config test has no skip on policy-bundle download failure, so an offline dev machine with trivy fails `pnpm test`.","failure_scenario":"Developer with trivy installed but no network: trivy config policy-bundle download fails → test asserts result.ok===true → suite fails despite the issue mandating offline-stable tests."}
- {"file":"packages/plugin-trivy/src/binary.ts","line":25,"summary":"resolveInPath uses statSync(...).isFile() without an executable-bit check on POSIX; a non-executable 'trivy' file in PATH resolves and spawn yields EACCES (core maps it to BinaryNotFound, so the failure is normalized, but the resolution is still wrong and untested).","failure_scenario":"PATH contains a mode-0000 file named 'trivy' → resolveInPath returns it as the binary → spawn EACCES → BinaryNotFound with a misleading 'install trivy' hint."}
