{
  "name": "@dsh-forge/plugin-semgrep",
  "version": "0.2.0",
  "description": "Semgrep tools for DeepSeek Harness: scan, scan file, ruleset validate, security scan (read-only, findings to Diagnostic[])",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
