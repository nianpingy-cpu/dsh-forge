{
  "name": "@dsh-forge/plugin-vision",
  "version": "1.0.0",
  "description": "Vision, data analysis & chart tools for DeepSeek Harness: image inspection heuristics (PNG/JPEG/WebP/GIF/BMP), CSV/JSON analysis, and pure-SVG chart generation",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "dist",
    "scripts/vision-worker.mjs"
  ],
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
