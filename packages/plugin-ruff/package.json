{
  "name": "@dsh-forge/plugin-ruff",
  "version": "0.2.0",
  "description": "Ruff tools for DeepSeek Harness: lint check, format check, rule explain, fix, format",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
