{
  "name": "@dsh-forge/plugin-docker",
  "version": "0.2.0",
  "description": "Docker tools for DeepSeek Harness: status, ps, images, inspect, logs, compose status (read-only)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
