{
  "name": "@dsh-forge/core",
  "version": "0.2.0",
  "description": "Core SDK for dsh-forge plugins: process runner, diagnostics, workspace policy, contract test kit",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "publishConfig": {
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts"
  },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
