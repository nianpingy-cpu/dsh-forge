{
  "name": "@dsh-forge/plugin-trivy",
  "version": "0.2.0",
  "description": "Trivy tools for DeepSeek Harness: repo/config/secret/image scan and SBOM (findings to Diagnostic[], result types distinguished)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
