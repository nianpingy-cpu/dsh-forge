{
  "name": "@dsh-forge/plugin-ffmpeg",
  "version": "0.2.0",
  "description": "FFmpeg tools for DeepSeek Harness: probe, clip, transcode, concat, audio extract/convert, thumbnail, compress (workspace-write with overwrite guard)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
