{
  "name": "@dsh-forge/plugin-k6",
  "version": "0.2.0",
  "description": "k6 tools for DeepSeek Harness: version, run, smoke, load, stress, summary, threshold check (load testing)",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean"
  },
  "dependencies": {
    "@dsh-forge/core": "workspace:*"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
